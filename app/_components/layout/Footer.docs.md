## Changes

### Changes 1

**Problem:** In this [version](https://github.com/Calcifer077/portfolio-v2/blob/main/app/_components/layout/Footer.tsx), the middle element (social links) were not truly in center.

**Solution:** To resolve this issue, we used `flex-1` after `md`. `flex-1` makes all three columns of equal width and than for each column we set their alignment.
